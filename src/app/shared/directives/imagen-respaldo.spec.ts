import { ImagenRespaldo } from './imagen-respaldo';

describe('ImagenRespaldo', () => {
  it('should create an instance', () => {
    const directive = new ImagenRespaldo();
    expect(directive).toBeTruthy();
  });
});
