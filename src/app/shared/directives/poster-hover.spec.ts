import { PosterHover } from './poster-hover';

describe('PosterHover', () => {
  it('should create an instance', () => {
    const directive = new PosterHover();
    expect(directive).toBeTruthy();
  });
});
